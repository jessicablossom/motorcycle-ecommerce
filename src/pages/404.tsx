import Layout from '@/app/layout';

const NotFound = () => {
	return (
		<Layout>
			<div className='flex min-h-screen flex-col items-center justify-between p-24'>Not Found</div>
		</Layout>
	);
};

export default NotFound;
